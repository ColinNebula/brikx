{
  "name": "brikx",
  "version": "0.3.0",
  "private": true,
  "homepage": "/",
  "dependencies": {
    "@testing-library/dom": "^10.4.1",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.1",
    "@testing-library/user-event": "^13.5.0",
    "react": "^19.2.3",
    "react-dom": "^19.2.3",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "security-audit": "node prepare-github.js",
    "prepare-github": "npm audit && node prepare-github.js",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "deploy-netlify": "npm run build && netlify deploy --prod --dir=build",
    "deploy-vercel": "npm run build && vercel --prod",
    "deploy-azure": "npm run build",
    "validate-pwa": "echo Visit https://www.pwabuilder.com and enter your deployed URL"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "~4.9.5"
  }
}
